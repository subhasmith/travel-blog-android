<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.barkside.travellocblog"
    android:versionCode="6"
    android:versionName="2.0.0" >

    <permission
        android:name="com.barkside.travellocblog.permission.MAPS_RECEIVE"
        android:protectionLevel="signature" />

    <uses-permission android:name="com.barkside.travellocblog.permission.MAPS_RECEIVE" />
    <!--
         Copied from Google Maps Library/AndroidManifest.xml.
    	 Android 2.3.3 Gingerbread is SDK Version 10, and Android 4.3 Jelly Bean is SDK 18
    -->
    <uses-sdk
        android:minSdkVersion="10"
        android:targetSdkVersion="18" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <!-- External storage for caching. -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- My Location -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- Maps API needs OpenGL ES 2.0. -->
    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />
    <!-- End of copy. -->

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/travel_icon"
        android:label="@string/app_name" >
        <uses-library android:name="com.google.android.maps" />
        <!--
         ** You need to replace the key below with your own key. **
            If it is: "AIza...yauM", then it is a debug key on aczoom Windows machine
        -->
        <meta-data
            android:name="com.google.android.maps.v2.API_KEY"
            android:value="AIzaSyBXvuyGT1W85epkj9Rr0Z3i19iwo0lNBK8"
            />

        <activity
            android:name="com.barkside.travellocblog.TravelLocBlogMain"
            android:label="@string/app_name"
            android:screenOrientation="portrait" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.barkside.travellocblog.EditBlogElement"
            android:configChanges="orientation" />
        <activity
            android:name="com.barkside.travellocblog.EditLocation"
            android:configChanges="orientation" />
        <activity
            android:name="com.barkside.travellocblog.TripMapView"
            android:configChanges="orientation" />
        <activity
            android:name="com.barkside.travellocblog.AboutActivity"
            android:label="@string/app_name" />
    </application>

</manifest>
